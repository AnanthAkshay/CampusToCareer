# Example local environment mapping for development only.
# Do not rename this file to db.properties.
# Configure the real values with environment variables instead:
#   DB_URL
#   DB_USER
#   DB_PASSWORD

DB_URL=jdbc:mysql://localhost:3306/rit_placement
DB_USER=root
DB_PASSWORD=change_me
